<Window x:Class="ETMPClient.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ETMP Player" 
        Height="500" Width="500"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ShowInTaskbar="False"
        Loaded="Window_Loaded">
    
    <Window.Resources>
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetName="MainBorder"
                           Storyboard.TargetProperty="Opacity"
                           From="0" To="1"
                           Duration="0:0:0.5"/>
        </Storyboard>
        
        <Storyboard x:Key="FadeOutStoryboard" Completed="FadeOut_Completed">
            <DoubleAnimation Storyboard.TargetName="MainBorder"
                           Storyboard.TargetProperty="Opacity"
                           From="1" To="0"
                           Duration="0:0:0.3"/>
        </Storyboard>
    </Window.Resources>
    
    <Border x:Name="MainBorder" 
            Background="#000000" 
            CornerRadius="20"
            Opacity="0">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="40" ShadowDepth="0" Opacity="0.8"/>
        </Border.Effect>
        
        <Grid>
            <!-- Logo -->
            <Image Source="/Images/etmp_logo.png" 
                   Width="350" Height="350"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>
            
            <!-- Loading indicator -->
            <StackPanel VerticalAlignment="Bottom" Margin="0,0,0,40">
                <!-- Progress bar -->
                <Border Height="3" Width="200" Background="#20FFFFFF" CornerRadius="1.5" Margin="0,0,0,15">
                    <Border x:Name="ProgressBar" 
                            Background="{StaticResource Brush.Accent.Primary}" 
                            CornerRadius="1.5"
                            HorizontalAlignment="Left"
                            Width="0">
                        <Border.Effect>
                            <DropShadowEffect Color="{StaticResource Color.Accent.Primary}" BlurRadius="10" ShadowDepth="0" Opacity="0.6"/>
                        </Border.Effect>
                    </Border>
                </Border>
                
                <!-- Loading text -->
                <TextBlock Text="Loading..." 
                           FontFamily="{StaticResource Urbanist}"
                           FontSize="12"
                           Foreground="#60FFFFFF"
                           HorizontalAlignment="Center"
                           Opacity="0.6"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
